<%= simple_form_for [:admin, level] do |f| %>
  <%= f.input :name %>
  <div class="field">
    <%= f.label :description %>
    <%= f.rich_text_area :description %>
  </div>
  <%= f.association :game %>
  <%= f.submit %>
<% end %>
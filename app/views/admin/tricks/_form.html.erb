<%= simple_form_for [:admin, trick] do |f| %>
  <%= f.input :name %>
  <%= f.input :game_version %>
  <%= f.input :discoverer %>
  <%= f.input :platform %>
  <div class="field">
    <%= f.label :description %>
    <%= f.rich_text_area :description %>
  </div>
  <%= f.submit %>
<% end %>